// Placeholder credentials, hardcoded for now — replace before deploying anywhere
// beyond localhost. ADMIN_PASSWORD is intentionally blank for testing; app/admin/page.tsx
// surfaces a warning after login for as long as that remains true.
export const ADMIN_USERNAME = 'admin'
export const ADMIN_PASSWORD = ''
